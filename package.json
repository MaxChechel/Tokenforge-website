{
  "name": "tokenforge-website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild './src/**/*' --bundle --minify --outdir=dist --watch",
    "build": "esbuild './src/**/*' --bundle --minify --outdir=dist"
  },
  "author": "Maksym Chechel",
  "license": "ISC",
  "dependencies": {
    "gsap": "npm:@gsap/shockingly@^3.12.5",
    "split-type": "^0.3.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
